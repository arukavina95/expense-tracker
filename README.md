# Expense Tracker 💸

A modern personal expense tracking web application built with **React**, **TypeScript**, and **Supabase**.

The app allows authenticated users to track daily expenses, view monthly summaries, analyze spending by category, and manage personal settings such as budget and default categories.

---

## ✨ Features

- 🔐 Secure authentication (Supabase Auth)
- 💾 Cloud database with Row Level Security (RLS)
- 📊 Dashboard with monthly totals, daily average, and budget usage
- 🧾 Expense management (add, delete, list expenses)
- 📈 Analytics by category
- ⚙️ User settings (currency, monthly budget, default category)
- 📱 Responsive UI (desktop & mobile ready)

---

## 🛠 Tech Stack

- **Frontend:** React + TypeScript (Vite)
- **Backend / DB:** Supabase (PostgreSQL + Auth + RLS)
- **Styling:** Custom CSS (modern card-based UI)
- **Deployment:** Vercel (recommended)

---

## 🚀 Getting Started

### 1. Install dependencies
```bash
npm install
